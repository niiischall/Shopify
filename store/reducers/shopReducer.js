const initialState = {
    categories: [],
    products: [],
    orders: []
}

const shopReducer = (state=initialState, action) => {
    switch(action.type){
        default: 
            return state;
    }
}

export default shopReducer;